const reportWebVitals = (onPerfEntry) => {
  // This is a simplified version that doesn't rely on web-vitals
  if (onPerfEntry && onPerfEntry instanceof Function) {
    // We could add actual performance measurements here in the future
    console.log('Performance measurement is disabled');
  }
};

export default reportWebVitals;
